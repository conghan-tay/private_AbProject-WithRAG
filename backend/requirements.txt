# Abnormal File Vault — Python Dependencies
# Target runtime : Python 3.10.19
# Docker base    : python:3.10.19-slim-bookworm
# Generated      : May 2026
#
# All versions conflict-checked and pinned to exact resolved versions.
# See DEPENDENCIES.md for full rationale.

# ---------------------------------------------------------------------------
# Core (starter project)
# ---------------------------------------------------------------------------
Django==4.2.30
djangorestframework==3.17.1
gunicorn==26.0.0
python-dotenv==1.2.2
whitenoise==6.12.0
pathspec==0.11.2
requests==2.33.1
pytest==8.3.5

# ---------------------------------------------------------------------------
# Feature additions (PRD Section 11)
# ---------------------------------------------------------------------------
cryptography==46.0.6
python-magic==0.4.27
django-filter==25.1           # pinned: 25.2 requires Python>=3.10 AND Django>=5.2

# ---------------------------------------------------------------------------
# NICE (uncomment to enable)
# ---------------------------------------------------------------------------
# drf-spectacular==0.29.0
redis==7.4.0

# ---------------------------------------------------------------------------
# Transitive dependencies (pinned for full reproducibility)
# ---------------------------------------------------------------------------
asgiref==3.11.1
sqlparse==0.5.5
